---
import YouTubeLiteEmbed, {
	type YouTubeLiteEmbedProps
} from './YouTubeLiteEmbed'

interface Props extends YouTubeLiteEmbedProps {}
const { id, title, cover, poster } = Astro.props
---

<YouTubeLiteEmbed client:idle {id} {title} {poster} {cover} />

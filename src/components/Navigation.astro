---
import { navigation } from '@/data/navigation'
import Link from '@/components/ui/Link.astro'
import type { NavigationMenu } from '@/data/types'

// interface Props extends HTMLAttributes<'a'> {
interface Props {
	block?: boolean
	class?: string
	// size?: 'md' | 'lg'
	style?: 'horizontal' | 'vertical'
}

const styles = {
	horizontal: 'flex-row uppercase font-heading font-medium',
	vertical: 'flex-col font-black text-4xl uppercase font-heading mt-10'
}

const {
	// size = 'md',
	style = 'horizontal',
	// block,
	class: className,
	...rest
} = Astro.props
---

{
	navigation && (
		<nav>
			<ul
				class:list={[
					'flex gap-4',
					// block && 'w-full',
					// sizes[size],
					styles[style],
					className
				]}
				{...rest}
			>
				{navigation.map((item: NavigationMenu) => (
					<li>
						<Link href={item.path}>{item.title}</Link>
					</li>
				))}
			</ul>
		</nav>
	)
}
